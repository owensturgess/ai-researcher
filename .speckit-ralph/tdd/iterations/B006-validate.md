The B006-red.md file contains only the same description the user sent in their message — there is no actual test code in either file.

**I cannot perform a blind validation without test code to review.**

The B006-red.md file contains only a verbal description of the intended fixes, not the proposed test implementation. To complete the validation, I need the actual test file content — the Python code with the test function(s), fixtures, mocks, and assertions.

Please provide the proposed test code for `tests/unit/test_pipeline_run_metadata.py` and I will run it through all 8 checklist items.
