There are no `src/` or `tests/` directories — no implementation source code or test files exist yet. The prompt itself confirms this:

> ## Current Implementation
> (No implementation source code found)

> ## Current Tests
> (No test files found)

# No Refactoring Needed

There is no implementation code to refactor. The `src/` and `tests/` directories don't exist yet. Refactoring applies to existing code — once implementation begins (after RED → GREEN cycles produce code), this step will have material to work with.
